import hero from '../assets/hero4.jpg'

const Hero = () => {
    return (
        <div className='hero'>
            <img src={hero} alt='hero' className='hero_img' />
        </div>
    )
}

export default Hero
